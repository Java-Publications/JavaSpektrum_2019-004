package org.rapidpm.publication.javasprektrum.api;

import java.util.Date;

public interface ServiceAPI {
  String toUpperCase(String input);
  String wasteMemory();
  String formatDate(Date date);
}
